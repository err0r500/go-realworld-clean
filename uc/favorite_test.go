package uc_test

import (
	"testing"
)

func TestInteractor_FavoritesUpdate(t *testing.T) {
	//fixme : todo
	//mockCtrl := gomock.NewController(t)
	//defer mockCtrl.Finish()
	//
	//rick := testData.User("rick")
	//janeArticle := testData.Article("janeArticle")
	//
	//i := mock.NewMockedInteractor(mockCtrl)
	//i.TagsRW.EXPECT().GetAll().Return(tags, nil).Times(1)
	//
	//retArticle, err := i.GetUCHandler().FavoritesUpdate(rick.Name, janeArticle.Slug, true)
	//
	//assert.NoError(t, err)
	//assert.Equal(t, tags, retArticle)
}
